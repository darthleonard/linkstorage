import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darthleonard.linkstorage',
  appName: 'linkstorage',
  webDir: 'www'
};

export default config;
